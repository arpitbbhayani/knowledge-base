MySQL DATE, DATETIME, and TIMESTAMP

Datetime and Timestamp which one to use and when? Restrictive K DATE:stores date butnottime (1000-01-01 to 9999-12-31) DATETIME:stores date and time (1000-01-01 00:00:00 to 9999-12-31 23:59:59) TIMESTAMP:stores date and time (unixepoch as integer (1970-01-01 00:00:00 UTC to 2038-01-19 03:14:07 UTC) O to 2seconds) atmicroseconds what ifwe want to stone level granularity? 4 DATETIMEand TIMESTAMP supports microseconds (additional) 1000 - 01-01 00:00:00.000000 to Fractional seconds take up 9999-12-3123:59:59.999999 0 to 3 bytes depending on precision Oprecision -> 0 bytes Storage Requirements > 1.2 precision -> 1 byte DATE 3 bytes BATETIME 5 bytes + fractional seconds TIMESTAMP 4 bytes + fractional seconds

DATETIME convenience - Use DATETIME When you want to store usecase specific time eg:appointment, schedule - at (specific and static) do calculations within you want When to mysal eg:DATE_ADD) purchased at,' - INFERUAL 1 BAY DATETIME is stored as a compact 5 byte representation (Mysal) but longer range than timestamp covering a Human-readable and native language objectsupport Queries and console output TIMESTAMP Timestamps are stored as integers representing time in UTC *timezone conversion happens during return a accepting on disk * it is justUTC return value with the connection timezone changes Timestamps are lightweight than Datetime on storage and index (1 byte better) when want to record time Use timestamps you system eg:created - at, transaction time, etc.